import React from 'react';

const FooterAll = ({footerData}) => {
    console.log(footerData.id)
    
    return (
        <div className='flex gap-4'>
            
        </div>
    );
};

export default FooterAll;